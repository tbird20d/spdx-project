Here are some dev notes and bugs for the python version of scan-build-log

= To do: =
 - parse 'printf' lines
   - pattern: rm -rf ../built-in.a; printf ... | xargs ar cDPrST ../built-in.a
 - parse 'ar' lines
   - pattern: rm -rf ../built-in.a; ar cDPrST ../built-in.a

 - parse 'strip' lines
 - ignore GCC heuristics lines
 - ignore compiled by lines
 - ignore shell fragments

 - suppress generated files
 - determine difference between python and .sh outputs

= pending issues =

== output includes .o files ==
should remove .o files from output

== output doesn't match scan-build-log.sh ==
see scan-with-sh.txt (output from scan-build-log.sh)

##############################################
= don't care issues =
== sub-tool cc1 uses .file.o.d to create /tmp/foo.s file
Line 79 is an invocation of cc1 (sub-tool) to convert
the .c to a .s file


##############################################
= FIXED issues =
== inclusion of header files in outputs list ==
-- FIXED --

Line 69: build line for build of scripts/mod/empty.c
 build ojectio: outputs var include header files
  ./include/linux/compiler-version.h
  ./include/linux/kconfig.h
  ./include/linux/compiler_types.h

But these are arguments of "-include", and should be int he sources list

== build line for empty.c build has 'as' build line ==
Fixed - parse 'as' as a valid build tool

== cc1 output with search list is not parsed correctly =
Line 233: cc1 invocation (with no apparent source) outputs include search dirs:
 #include "..." search starts here:
 #include <...> search starts here:
  ./arch/x86/include
  ./arch/x86/include/generated
  ...

which the scanner doesn't ignore

Fixed: search starts here added to set of ignore block start phrases

== cc1 line sources include .o files
line 312: cc1 for init/main.c lists init/main.o, init/.main.o.d in sources

init/.main.o.d is the argument of -MMD
init/main.o is the argument of -auxbase-strip

